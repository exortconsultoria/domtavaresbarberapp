# Configuração local do MCP (aponta pro Supabase; não faz parte do site)
.mcp.json

# Backups exportados pelo app
dom-tavares-*.json

# servidor e config locais de teste (nao fazem parte do site)
servidor-local.js
.claude/
